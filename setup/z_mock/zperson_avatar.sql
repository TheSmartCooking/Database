-- Use the database
USE smartcooking;

-- Fill the database with mock data
CALL insert_person_avatar(1, 1);
CALL insert_person_avatar(2, 2);
CALL insert_person_avatar(3, 1);
CALL insert_person_avatar(4, 3);
